# Server configuration
server.port=8080

# MongoDB configuration (adjust as needed)
spring.data.mongodb.uri=mongodb://localhost:27017/ocean_view_resort

# Spring application name
spring.application.name=ocean-view-resort

# Logging level (you can tune this)
logging.level.org.springframework=INFO

# Directory for uploaded room images (relative to working directory)
app.upload-dir=uploads

